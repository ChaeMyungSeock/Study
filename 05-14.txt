분류와 회귀 방식

변화하는 주가는 => 회귀

if 분류로 할 경우 가격을 구간별로 나눔, => 오르면 1, 떨어지면 0
   종목별로 분류 => y값을 정해야 함

모델링, 6만장의 데이터(이미지) => 적다면? 97% // 데이터가 많다면 성능이 좋아짐 따라서 데이터를 증폭시킴

문제를 풀면서 답안지를 보고 공부하면 능률 증가하겠지?
보통 모델을 평가하는 데이터는 훈련에 포함되지 못하는 데이터가 된다. 따라서 훈련하면서 검증해라
val => 훈련하면서 검증한다
train 1~7 // 가중치만 계산
val 8~10 => 머신의 test set but train에 포함되어서 훈련함// 가중치가 변화하면서 검증 => fit에서 작동 // 가중치 향상 (더 잘나옴)

test 11~15 => evaluate // 사람이 보는 검증(모델 평가)
pred 16~18

